<Window x:Class="MySSH.Views.TakeoverTargetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="首次接管"
        Width="440"
        MinWidth="420"
        MinHeight="210"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        Background="#F4F7FB">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1" Background="White" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="IP 或域名"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"/>
                <TextBox x:Name="HostTextBox"
                         Grid.Row="1"
                         Height="34"
                         VerticalContentAlignment="Center"/>
                <TextBlock x:Name="ErrorTextBlock"
                           Grid.Row="2"
                           Foreground="#B91C1C"
                           Margin="0,8,0,0"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="取消" IsCancel="True" MinWidth="80"/>
            <Button Content="生成脚本" Click="Generate_Click" Style="{StaticResource PrimaryButtonStyle}" MinWidth="92"/>
        </StackPanel>
    </Grid>
</Window>
