<Window x:Class="MySSH.Views.ServerEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="服务器"
        Width="540"
        Height="520"
        MinWidth="500"
        MinHeight="480"
        WindowStartupLocation="CenterOwner"
        Background="#F4F7FB">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1" Background="White" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Alias" VerticalAlignment="Center" Margin="0,0,10,10"/>
                <TextBox Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Alias, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="1" Text="Host" VerticalAlignment="Center" Margin="0,0,10,10"/>
                <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="2" Text="Port" VerticalAlignment="Center" Margin="0,0,10,10"/>
                <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="3" Text="User" VerticalAlignment="Center" Margin="0,0,10,10"/>
                <TextBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding User, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="4" Text="Key path" VerticalAlignment="Center" Margin="0,0,10,10"/>
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding KeyPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,10"/>
                <Button Grid.Row="4" Grid.Column="2" Content="选择" Click="BrowseKey_Click" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="5" Text="Tags" VerticalAlignment="Center" Margin="0,0,10,10"/>
                <TextBox Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding TagsText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="6" Text="Notes" Margin="0,2,10,0"/>
                <TextBox Grid.Row="6"
                         Grid.Column="1"
                         Grid.ColumnSpan="2"
                         Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="Wrap"
                         MinHeight="110"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="取消" IsCancel="True" MinWidth="80"/>
            <Button Content="保存" Click="Save_Click" Style="{StaticResource PrimaryButtonStyle}" MinWidth="80"/>
        </StackPanel>
    </Grid>
</Window>
