# My SSH Windows 桌面服务器安全工具计划

## Summary
- 产品名：`My SSH`，EXE：`MySSH.exe`。
- 定位：Windows 原生服务器安全工具，首版聚焦 `SSH 连接`、`服务器信息管理`、`服务器密钥管理`。
- 技术栈：`.NET 10 + WPF`，发布为 win-x64 self-contained 单文件 EXE，并同时发布 SHA256 校验值。
- 密钥接管流程改为：本机生成 key 和复制命令，服务器从 GitHub Release 下载静态脚本并临时开放 SSH 端口，本机主动 SSH 验证，成功后关闭临时入口。

## UI 与交互
- 主界面采用左侧导航 + 右侧工作区，专业紧凑风格，主题跟随系统。
- 首次启动显示空服务器列表和三个入口：新增服务器、导入 SSH config、服务器密钥接管。
- 服务器列表用表格展示，支持搜索、标签筛选、编辑、删除、测试连接。
- 主页模块：
  - `SSH 连接`：内置多标签 SSH 终端，体验目标接近 Termius。
  - `服务器信息管理`：管理连接资料、标签、备注和静态机器配置。
  - `服务器密钥管理`：按服务器执行首次接管、验证、轮换、禁用密码登录。
- 密钥管理使用单页工作台，不做重向导；临时 SSH 端口细节默认隐藏，只显示复制命令、探测状态、验证状态和收尾结果。

## 核心实现
- 本地数据保存到 `%APPDATA%\My SSH\servers.json`，保存 alias、host、port、user、key 路径、标签、备注、最后连接状态、OS、CPU 核心数、内存总量、磁盘总量。
- 不保存服务器密码、sudo 密码、私钥口令或私钥正文；密码/口令在连接表单输入，只在当前会话内使用。
- 支持从 `%USERPROFILE%\.ssh\config` 导入 Host，用户选择后写入服务器列表。
- 内置 SSH 终端支持 shell 交互、多标签、复制粘贴、搜索、字体/主题设置、resize、断开重连、会话日志开关。
- 首次连接新主机时显示 host key fingerprint，用户确认后写入 known_hosts。
- 连接成功后采集静态机器配置快照，不采集实时 CPU/内存占用趋势。
- 设置页提供手动检查更新，分别检查 My SSH 应用版本和 GitHub 脚本 Release 版本。

## 密钥接管与轮换
- GitHub 使用公开仓库发布静态脚本，复制命令固定下载 Release/tag 版本，不使用 main/latest 作为默认执行源。
- 本机生成 ed25519 私钥/公钥；私钥永远留在本机，公钥 base64 后放入复制命令。
- 本机生成一次性 token 和随机高位临时 SSH 端口；token 只用于标识本次操作和校验脚本输出，不用于派生私钥。
- 服务器脚本要求 root 或 sudo 权限，负责：
  - 备份 `authorized_keys`、sshd 配置和防火墙相关改动。
  - 安装新公钥。
  - 临时让 sshd 监听随机端口。
  - 尝试处理 `ufw`、`firewalld`、`iptables` 本机防火墙。
  - reload sshd 并输出脱敏结果。
- 云安全组不做自动集成，UI 提示用户自行放行临时端口。
- My SSH 自动探测服务器临时端口；端口打开后用本机私钥 SSH 登录验证。
- 验证成功后，正式 SSH 配置保留原端口，只更新 key 登录能力和本机 SSH config。
- 临时端口在正式端口 + 新 key 验证成功后立即关闭。
- `authorized_keys` 默认替换为 My SSH 新公钥；失败时自动恢复备份。
- 禁用密码登录默认勾选，但只有新 key 已验证可连接后才解锁执行；v1 优先支持 Ubuntu/Debian 和 cloud-init 覆盖处理。
- key 轮换按同样流程生成 staged key；新凭证验证成功后替换正式 key，失败则恢复旧 key 和远端备份。

## 安全与恢复
- 危险操作不靠重复弹窗堆确认，统一用状态门槛控制：只有“新凭证已验证可连接”后才允许替换、禁用密码、关闭临时入口。
- 保存脱敏操作日志，记录时间、服务器、步骤、错误和恢复结果；隐藏 token、密码、私钥、公钥完整内容。
- 失败时自动恢复关键文件，并在 UI 中显示恢复是否成功和远端备份路径。
- 如果临时端口探测失败，UI 给出修复建议：检查云安全组、服务器防火墙、sudo 权限、sshd reload、host/IP 是否正确。
- 如果 alias 与非 My SSH 管理的 SSH config 冲突，阻止写入并要求用户换 alias。
- 已有同名本机 key 时，必须确认后备份，再生成新 key。

## Test Plan
- 发布测试：生成单个 `dist\MySSH.exe` 和 SHA256；无 SDK/源码文件依赖即可运行。
- UI 测试：空状态、侧栏导航、服务器表格、标签筛选、多标签终端、主题跟随系统正常。
- 数据测试：新增/编辑/删除服务器，导入 SSH config，重启后数据恢复。
- SSH 测试：key 登录、密码登录、host key 确认、失败提示、连接后静态机器配置采集。
- 密钥接管测试：Ubuntu/Debian 服务器执行 GitHub Release 脚本，临时端口打开，本机用新 key 登录，正式端口验证成功，临时端口关闭。
- 失败恢复测试：token 错误、sudo 不足、端口被云安全组拦截、sshd 配置错误、key 验证失败时，关键备份可恢复。
- 轮换测试：新 key 成功后旧 key 备份，新 key 失败时旧 key 仍可登录。
- 安全测试：日志脱敏，本地不保存密码/口令，私钥不出本机，复制命令不包含私钥。

## Assumptions
- 当前 workspace 作为项目目录。
- 开发机需要安装 .NET 10 SDK。
- GitHub 脚本仓库 URL 作为发布配置项写入应用，正式发布时固定到具体 Release/tag。
- v1 不做云厂商安全组 API 集成，不做实时监控，不做完整凭证保险库。
